apiVersion: v1
kind: Service

metadata:
  name: prometheus
  namespace: lgtm

spec:
  selector:
    app: prometheus


  ports:
      port: {{ .Values.prometheus.service.port }}
      targetPort: {{ .Values.prometheus.service.port }}