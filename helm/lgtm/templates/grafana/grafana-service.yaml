apiVersion: v1
kind: Service

metadata:
  name: grafana
  namespace: lgtm

spec:
  selector:
    app: grafana

  ports:
    - port: {{ .Values.grafana.service.port }}
      targetPort: {{ .Values.grafana.service.port }}