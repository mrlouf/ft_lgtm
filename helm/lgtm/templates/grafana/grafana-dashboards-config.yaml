apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-dashboards
  namespace: lgtm

data:
  application.json: |
{{ .Files.Get "files/grafana/dashboards/application.json" | indent 4 }}
  traces.json: |
{{ .Files.Get "files/grafana/dashboards/traces.json" | indent 4 }}
  