apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-provisioning
  namespace: lgtm

data:
  dashboards.yaml: |
{{ .Files.Get "files/grafana/provisioning/dashboards/dashboards.yaml" | indent 4 }}
  datasources.yaml: |
{{ .Files.Get "files/grafana/provisioning/datasources/datasource.yaml" | indent 4 }}