apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: lgtm

spec:
  selector:
    app: frontend

  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.port }}