global:
  domain: argocd.lgtm.local

server:
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - argocd.lgtm.local
    path: /

configs:
  params:
    server.insecure: "true"